const prompt = require("prompt-sync")({ sigint: true});
//calcular o fatorial de um número. Escreva uma Função que receba um número como parâmetro e retorne o fatorial desse número
function fatorial(a){
    let fator=1
    for(var i = n1 ; i > 0 ; i--){
fator = fator*i
console.log(fator);
    }
}
let n1 = Number(prompt(`Digite um número: `));
fatorial(n1);